query PageEmailsFrom($address: EmailAddress!) @loading {
	address(email: $address) {
		...Avatar
		name
		address

		sentEmails(first: 10) @paginate {
			edges {
				node {
					id
					subject
					receivedAt
				}
			}
		}
	}
}
