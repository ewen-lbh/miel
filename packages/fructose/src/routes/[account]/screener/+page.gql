query PageScreener($account: EmailAddress!) @loading(cascade: true) {
	account(address: $account) {
		inboxes(type: [FEED, INBOX, TRASHBOX]) {
			id
			name
			type
		}
	}
	screenings(first: 5) @paginate {
		edges {
			node {
				id
				address
				...Avatar
				name
				probablyAPerson
				verified
				lastSentEmails: sentEmails(first: 3) {
					nodes {
						...EmailRow
					}
				}
			}
		}
	}
}
