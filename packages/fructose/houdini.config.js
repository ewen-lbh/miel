/// <references types="houdini-svelte">

/** @type {import('houdini').ConfigFile} */
const config = {
    "schemaPath": "../invertase/schema.gql",
    "plugins": {
        "houdini-svelte": {}
    }
}

export default config
